# Worker-python

Python parsing/normalization worker for Shore Sentinel scanner output. It exposes `GET /health` and `POST /parse`, validates scanner contract version `shore-sentinel.scanner-output/v1`, normalizes findings, and returns an enrichment summary.
