import Link from 'next/link';import {Header,Filters,Pill} from '@/components/ui';import {audits,apiBase} from '@/lib/data';import {routePath} from '@/lib/paths';
export default function Audits(){return <div className='stack'><Header eye='Audit History' title='One-time audits stay outside fleet health' desc='Use this view for ad hoc validation, evidence, and temporary target reviews.'><Link className='btn' href={routePath('/audits/new')}>Run One-Time Audit</Link></Header><Filters name='Audit history' items={['Severity','Time range','Environment']}/><p className='note'>API list: {apiBase}/audits?asset_mode=one_time_audit</p><section className='panel'>{audits.map(a=><Link className='row' href={routePath('/audits/'+a.id)} key={a.id}><span><b>{a.target}</b><small>{a.summary}</small></span><span>{a.promote}</span><Pill>{a.status}</Pill></Link>)}</section></div>}
