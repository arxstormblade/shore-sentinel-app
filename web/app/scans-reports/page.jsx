import Link from 'next/link';import {Header,Filters,Pill} from '@/components/ui';import {reports,apiBase} from '@/lib/data';import {routePath} from '@/lib/paths';
export default function Scans(){return <div className='stack'><Header eye='Scans & Reports' title='Run scans, follow progress, review reports' desc='Execution, history, and report viewing share this surface. Comparison and import/export stay in secondary actions.'><Link id='audit-entry' className='btn' href={routePath('/audits/new')}>Run One-Time Audit</Link></Header><Filters name='Scans & Reports' items={['Severity','Time range','Environment']}/><p className='note'>API list: {apiBase}/reports</p><section className='panel'>{reports.map(r=><Link className='row' href={routePath('/scans-reports/reports/'+r.id)} key={r.id}><span><b>{r.title}</b><small>{r.source} · {r.env}</small></span><Pill>{r.status}</Pill><Pill>{r.severity}</Pill></Link>)}</section></div>}
