import Link from 'next/link';import {Header,Filters,Pill} from '@/components/ui';import {remediations,apiBase} from '@/lib/data';import {routePath} from '@/lib/paths';
export default function Remediation(){return <div className='stack'><Header eye='Remediation' title='Cross-cutting finding workflow' desc='Track findings from managed scans and one-time audit reports without dashboard clutter.'/><Filters name='Remediation' items={['Severity','Time range','Environment']}/><p className='note'>API list: {apiBase}/remediation</p><section className='panel'>{remediations.map(r=><Link className='row' href={routePath('/remediation/'+r.id)} key={r.id}><span><b>{r.title}</b><small>{r.asset} · {r.owner}</small></span><Pill>{r.severity}</Pill><Pill>{r.status}</Pill></Link>)}</section></div>}
