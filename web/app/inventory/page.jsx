import Link from 'next/link';import {Header,Filters,Pill} from '@/components/ui';import {machines,apiBase} from '@/lib/data';import {routePath} from '@/lib/paths';
export default function Inventory(){return <div className='stack'><Header eye='Inventory' title='Managed machines and environments' desc='Inventory is scoped to enrolled machines. Use environment and status filters only.'><Link id='add-managed-machine' className='btn' href={routePath('/inventory/new')}>Add Managed Machine</Link></Header><Filters name='Inventory' items={['Environment','Status']}/><p className='note'>API list: {apiBase}/machines?asset_mode=managed_machine</p><section className='cards'>{machines.map(m=><Link className='card' href={routePath('/inventory/machines/'+m.id)} key={m.id}><h2>{m.name}</h2><p>{m.summary}</p><dl><dt>Environment</dt><dd>{m.env}</dd><dt>Score</dt><dd>{m.score}</dd></dl><Pill>{m.status}</Pill></Link>)}</section></div>}
